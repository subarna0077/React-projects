import React, { useRef } from 'react'


const StudentForm = ({}) => {

    const name = useRef();
    const number = useRef();
    const email = useRef();
    const date = useRef();

    const submitData = ()=> {

    }
  return (
    <div>
      
    </div>
  )
}

export default StudentForm